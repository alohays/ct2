{
  "manifest": "ct2-workflow-run/v1",
  "ticket": "ct2-example-fanout",
  "subagents": [
    {
      "id": "worker-1",
      "kind": "worker",
      "prompt": "Implement the parser fix for ticket ct2-example-fanout.",
      "output": "diff --git a/src/parser.py b/src/parser.py",
      "writes": ["src/parser.py"]
    },
    {
      "id": "verifier-cc",
      "kind": "verifier",
      "prompt": "Review this work product independently: diff --git a/src/parser.py b/src/parser.py",
      "output": "verdict: approve. Findings: none blocking."
    },
    {
      "id": "skeptic-1",
      "kind": "skeptic",
      "prompt": "The first reviewer said: verdict: approve. Findings: none blocking. Now review: diff --git a/src/parser.py b/src/parser.py",
      "output": "verdict: approve. Findings: agrees with first reviewer."
    }
  ]
}
