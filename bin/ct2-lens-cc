#!/usr/bin/env bash
# ct2-lens-cc — Dispatch the CT2 lens-cc role through an agent adapter.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${SCRIPT_DIR}/_ct2_lens_dispatch.py" lens-cc "$@"
